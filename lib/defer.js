var Keen = require('./index');

module.exports = {
  'deferEvent': deferEvent,
  'deferEvents': deferEvents
};

function deferEvent(eventCollection, eventModifier){

}

function deferEvents(events, callback){

}
